// Auth iş mantığı + veritabanı sorguları
// Modül 2'de implement edilecek

// register(), login(), refreshToken(), logout()
// verifyEmail(), forgotPassword(), resetPassword()
// getSessions(), revokeSession()
